Generate an SAT Math question involving linear equations in one or two variables.

The question should:
- Present a real-world scenario or abstract problem involving linear equations
- Require solving for a variable or finding a specific value
- Use appropriate difficulty-level complexity

Include:
- Clear problem setup with necessary constraints
- Realistic numbers and contexts
- Unambiguous solution path

{DIFFICULTY_DESCRIPTION}

CRITICAL REQUIREMENTS:

1. DESIGN BACKWARDS (PREVENTS MOST ERRORS):
   - FIRST: Choose your final answer (pick a clean integer between 10-200)
   - SECOND: Work backwards to create constraints that produce this answer
   - THIRD: Write out the complete forward solution with ALL arithmetic shown
   - FOURTH: Verify every single calculation (check each addition, subtraction, multiplication, division)
   - For difficulty 4-5: Start with the answer, then design the system to make the setup non-obvious
   - NEVER design forward by picking constraints first

2. MANDATORY ARITHMETIC VERIFICATION:
   - After designing backwards, solve forward as a student would
   - Write out EVERY arithmetic operation explicitly (e.g., "1100 + 1440 = 2540")
   - Double-check each calculation using a different method (e.g., verify 6660 - 5400 by computing 6660 - 5400 = 1260)
   - The computed answer MUST exactly match one of your four choices
   - If any step produces non-integers or messy fractions, redesign completely
   - Common error: claiming A + B = C without actually computing it—always verify

3. DISTRACTOR DESIGN - MATHEMATICAL PROOF REQUIRED:
   - Each distractor must result from applying ONE realistic, specific algebraic error
   - For each distractor, provide this exact structure:
     * "This results from [single specific error like 'adding equations instead of subtracting']"
     * Show the complete derivation: "2x + 3y = 50 PLUS 4x + 3y = 80 gives 6x + 6y = 130, so..."
     * Verify the calculation produces exactly the distractor value
   - FORBIDDEN rationales: "computational error," "incorrect setup," "proportion error" (too vague)
   - FORBIDDEN: distractors requiring multiple errors (e.g., "sign error then rounding")
   - FORBIDDEN: distractors that are non-integers (like 127.5 or 66⅔) unless the correct answer is also non-integer
   - Each distractor must be a clean integer if the correct answer is a clean integer
   - Test: Can you write out the exact wrong calculation that produces this distractor? If not, choose a different value

4. DIFFICULTY CALIBRATION - BE CONSERVATIVE:
   - 1-2/5: One equation, 1-3 algebraic steps, answer obvious from setup
   - 3/5: Standard two-equation system using substitution/elimination (4-6 steps total)
   - 4/5: Requires insight to set up equations OR non-standard manipulation strategy
   - 5/5: Multiple layers of abstraction or counterintuitive relationships
   - Standard elimination/substitution of a 2-equation system is 3/5 maximum
   - Clean arithmetic never increases difficulty
   - When uncertain, rate one level lower

5. EXPLANATION QUALITY:
   - Present only the clean, final solution path
   - NEVER include: "Wait," "Let me recalculate," "Actually," "Hmm," or any revision language
   - NEVER include crossed-out work or corrections
   - Structure: Setup equations → Solve step-by-step with explicit arithmetic → Verify answer
   - Show every calculation explicitly: write "15 × 80 = 1200" not "multiply to get 1200"
   - Each step should follow logically from the previous with no backtracking

6. AUTHENTICITY:
   - Use general timeframes ("recently," "last month")
   - Natural context without forced complexity
   - Numbers must fit the real-world scenario (no fractional people, seats, tickets, or discrete objects)
   - Avoid overly formulaic setups; vary contexts naturally

FINAL QUALITY CHECK BEFORE SUBMITTING:
□ Is the correct answer a clean integer (or clean decimal if appropriate)?
□ Did you verify EVERY arithmetic operation by actually computing it?
□ Does each distractor have a specific, single-error mathematical derivation?
□ Are all four answer choices clean integers (matching the correct answer's format)?
□ Did you solve the problem twice independently to confirm consistency?
